<UserControl x:Class="PrinterAgent.ConfigTool.StatusTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="700">
    <StackPanel Margin="0,12,0,0">
        <TextBlock Text="Status do client" Style="{StaticResource TitleText}" Margin="0,0,0,4"/>
        <TextBlock Text="Estado dos serviços deste computador." Style="{StaticResource SubtitleText}" Margin="0,0,0,16"/>

        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Serviço do Agent (link):" VerticalAlignment="Center"/>
                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                    <Ellipse x:Name="LinkServiceDot" Width="10" Height="10" Fill="Gray" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="LinkServiceStatusText" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Atualização automática:" Margin="0,10,0,0" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="Não implementada nesta versão" Margin="0,10,0,0"
                           Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <Button x:Name="RefreshStatusButton" Content="Atualizar status" Style="{StaticResource SecondaryButton}"
                HorizontalAlignment="Left" Margin="0,14,0,0" Click="RefreshStatusButton_Click"/>
    </StackPanel>
</UserControl>
