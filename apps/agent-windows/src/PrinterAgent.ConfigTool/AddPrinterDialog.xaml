<Window x:Class="PrinterAgent.ConfigTool.AddPrinterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Adicionar impressora por IP" Height="260" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Endereço IP" Style="{StaticResource SectionLabel}"/>
        <TextBox x:Name="IpTextBox" Grid.Row="1" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2" Text="Community SNMP" Style="{StaticResource SectionLabel}"/>
        <TextBox x:Name="CommunityTextBox" Grid.Row="3" Margin="0,0,0,4" Text="public"/>

        <TextBlock x:Name="StatusText" Grid.Row="4" Margin="0,8,0,0" TextWrapping="Wrap"
                   Foreground="{StaticResource MutedBrush}" FontSize="12" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="CancelButton" Content="Cancelar" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="CancelButton_Click"/>
            <Button x:Name="AddButton" Content="Buscar e adicionar" Style="{StaticResource PrimaryButton}" Click="AddButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
