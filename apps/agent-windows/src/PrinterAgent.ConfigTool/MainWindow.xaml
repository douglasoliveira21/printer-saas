<Window x:Class="PrinterAgent.ConfigTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrinterAgent.ConfigTool"
        mc:Ignorable="d"
        Title="Printer SaaS Agent" Height="660" Width="640"
        MinHeight="560" MinWidth="520"
        WindowStartupLocation="CenterScreen"
        Background="White"
        Icon="agent.ico">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Image Source="Assets/logo.png" Width="40" Height="40" Margin="0,0,12,0"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Printer SaaS Agent" Style="{StaticResource TitleText}"/>
                <TextBlock Text="Descoberta e monitoramento de impressoras na rede local" Style="{StaticResource SubtitleText}"/>
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="14" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <Ellipse x:Name="StatusDot" Width="12" Height="12" Fill="Gray" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock x:Name="StatusText" Text="Verificando status..." FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,14">
            <TextBlock Text="Servidor: " Style="{StaticResource SubtitleText}" Margin="0"/>
            <TextBlock x:Name="ApiUrlText" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource MutedBrush}"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,14">
            <TextBlock Text="Token de instalação" Style="{StaticResource SectionLabel}"/>
            <TextBlock Text="Gerado em: Configurações → Agents → Adicionar Agent, no SaaS. Só é necessário na primeira instalação."
                       Style="{StaticResource CaptionText}" TextWrapping="Wrap"/>
            <TextBox x:Name="EnrollmentTokenTextBox"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="0,0,0,14">
            <TextBlock Text="Redes para varredura (discovery)" Style="{StaticResource SectionLabel}"/>
            <TextBlock Text="Separe por vírgula. Ex.: 192.168.1.0/24, 192.168.2.100-192.168.2.200"
                       Style="{StaticResource CaptionText}" TextWrapping="Wrap"/>
            <TextBox x:Name="NetworksTextBox"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,8,0,18">
            <Button x:Name="InstallButton" Content="Instalar e Iniciar" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" Click="InstallButton_Click"/>
            <Button x:Name="StartButton" Content="Iniciar" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="StartButton_Click"/>
            <Button x:Name="StopButton" Content="Parar" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="StopButton_Click"/>
            <Button x:Name="UninstallButton" Content="Desinstalar" Style="{StaticResource SecondaryButton}" Click="UninstallButton_Click"/>
        </StackPanel>

        <DockPanel Grid.Row="6">
            <TextBlock DockPanel.Dock="Top" Text="Logs recentes" Style="{StaticResource SectionLabel}" Margin="0,0,0,8"/>
            <Border DockPanel.Dock="Top" Background="{StaticResource LogBackgroundBrush}" CornerRadius="6" Padding="1">
                <TextBox x:Name="LogTextBox" IsReadOnly="True" BorderThickness="0" VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="11" Padding="10"
                         Background="{StaticResource LogBackgroundBrush}" Foreground="{StaticResource LogForegroundBrush}" TextWrapping="NoWrap"/>
            </Border>
        </DockPanel>
    </Grid>
</Window>
