<UserControl x:Class="PrinterAgent.ConfigTool.SettingsTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="700">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
        <StackPanel>

            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="14" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Busca de novas impressoras" Style="{StaticResource SectionLabel}"/>
                    <CheckBox x:Name="NeverDiscoverCheckBox" Content="Nunca buscar automaticamente" Margin="0,4,0,8" Checked="NeverDiscoverCheckBox_Changed" Unchecked="NeverDiscoverCheckBox_Changed"/>
                    <StackPanel x:Name="DiscoveryIntervalPanel" Orientation="Horizontal">
                        <TextBlock Text="Buscar a cada" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBox x:Name="DiscoveryIntervalTextBox" Width="60"/>
                        <TextBlock Text="minutos" VerticalAlignment="Center" Margin="6,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="14" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Envio de heartbeat / alertas ao servidor" Style="{StaticResource SectionLabel}"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Enviar a cada" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBox x:Name="HeartbeatIntervalTextBox" Width="60"/>
                        <TextBlock Text="segundos" VerticalAlignment="Center" Margin="6,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="14" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Proxy" Style="{StaticResource SectionLabel}"/>
                    <TextBlock Text="Preencha somente se a rede exigir um proxy para acessar a internet." Style="{StaticResource CaptionText}" TextWrapping="Wrap"/>

                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="110"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,8">
                            <TextBlock Text="Servidor" Style="{StaticResource CaptionText}"/>
                            <TextBox x:Name="ProxyServerTextBox"/>
                        </StackPanel>
                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,8">
                            <TextBlock Text="Porta" Style="{StaticResource CaptionText}"/>
                            <TextBox x:Name="ProxyPortTextBox"/>
                        </StackPanel>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="Usuário" Style="{StaticResource CaptionText}"/>
                            <TextBox x:Name="ProxyUsernameTextBox"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,8,0">
                            <TextBlock Text="Senha" Style="{StaticResource CaptionText}"/>
                            <PasswordBox x:Name="ProxyPasswordBox"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Domínio" Style="{StaticResource CaptionText}"/>
                            <TextBox x:Name="ProxyDomainTextBox"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button x:Name="TestProxyButton" Content="Testar comunicação" Style="{StaticResource SecondaryButton}" Click="TestProxyButton_Click"/>
                        <TextBlock x:Name="ProxyTestStatus" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="12"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal">
                <Button x:Name="SaveButton" Content="Salvar" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="SaveButton_Click"/>
                <Button x:Name="SaveAndRestartButton" Content="Salvar e reiniciar serviço" Style="{StaticResource PrimaryButton}" Click="SaveAndRestartButton_Click"/>
            </StackPanel>
            <TextBlock x:Name="SaveStatus" Margin="0,8,0,0" FontSize="12" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap"/>

        </StackPanel>
    </ScrollViewer>
</UserControl>
